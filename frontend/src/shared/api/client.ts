/**
 * @deprecated · use `@/lib/api` · este arquivo só re-exporta para retro-compat.
 * Migração: trocar `import { api } from '@/shared/api/client'`
 *           por     `import { api } from '@/lib/api'`
 */
export { api } from '@/lib/api'
